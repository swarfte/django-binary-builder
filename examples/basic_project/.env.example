# Example environment file for the basic_project example.
# Copy this file to `.env` and replace the values.

DJANGO_SECRET_KEY=replace-this-with-a-long-random-value
DJANGO_ALLOWED_HOSTS=127.0.0.1,localhost
DJANGO_DEBUG=0
APP_FEATURE_REPORTING=true
